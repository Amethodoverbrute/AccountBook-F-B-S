1. **创建新的首页组件**：

   * 在 `src/components/` 目录下创建 `LandingPage.vue` 组件

   * 组件包含两部分：顶部导航栏和下方轮播图

   * 顶部导航栏：包含logo、"记账本"标题居中、登录|注册按钮

   * 轮播图：参考之前实现的轮播图功能

2. **修改路由配置**：

   * 修改 `src/router/index.js` 中的路由配置

   * 将根路径 ("/") 指向新创建的 `LandingPage` 组件

   * 移除根路径的 `requiresAuth` 元信息

   * 将原有 `Home` 组件的路径改为 `/dashboard`

   * 确保路由守卫配置正确

3. **实现新首页组件**：

   * 实现顶部导航栏，包含logo、标题和登录/注册按钮

   * 实现轮播图功能，包含自动播放和手动切换

   * 使用CSS样式美化页面，确保布局符合用户要求

   * 登录和注册按钮链接到相应的路由

4. **验证修改**：

   * 确保前端开发服务器正常运行

   * 访问 <http://localhost:5173/> 查看新首页

   * 验证登录和注册按钮的链接是否正确

   * 验证轮播图是否正常工作

5. **具体修改文件**：

   * `src/components/LandingPage.vue`：创建新的首页组件

   * `src/router/index.js`：修改路由配置

   * `src/components/Home.vue`：保留原有功能，调整路径

